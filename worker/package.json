{
  "name": "worker",
  "version": "1.0.0",
  "description": "Refrigerant Log MTS API (Cloudflare Worker + D1)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply refrigerant-log-mts --local",
    "db:migrate:remote": "wrangler d1 migrations apply refrigerant-log-mts --remote"
  },
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.13.1",
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
