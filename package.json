{
  "name": "refrigerant-log-mts",
  "version": "1.0.0",
  "description": "Digital refrigerant tracking log for EPA 608 compliance",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix worker && npm install --prefix client",
    "dev": "concurrently -n worker,client -c blue,green \"npm run dev --prefix worker\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "deploy": "npm run build && npm run deploy --prefix worker",
    "db:migrate:local": "npm run db:migrate:local --prefix worker",
    "db:migrate:remote": "npm run db:migrate:remote --prefix worker"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/solmasta/Refrigerant-Log---MTS.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/solmasta/Refrigerant-Log---MTS/issues"
  },
  "homepage": "https://github.com/solmasta/Refrigerant-Log---MTS#readme",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
